[tool.poetry]
name = "backend"
version = "0.1.0"
description = "Django環境構築用のパッケージ"
authors = ["dbd-fish"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"
django = "5.1.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
